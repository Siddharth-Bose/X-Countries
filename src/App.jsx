import React from "react";
import Countries from "./components/Countries.jsx";

// App Component: A stateless entry component that the application renders. It renders the Countries component inside it.
// Returns Type: JSX
function App() {
  return <Countries />;
}

export default App;
